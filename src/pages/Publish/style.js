import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    view:{
        flex: 1
    },
    text: {
        alignItems: 'center',
        justifyContent: 'center'
    }
})